{
  "name": "gigradar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal live-music event radar. Static frontend + GitHub Actions scraper pipeline, no build step (SPEC D14).",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "js-yaml": "^5.4.2"
  }
}
